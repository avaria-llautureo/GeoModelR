% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualise.R
\name{plot_clade}
\alias{plot_clade}
\title{Plot posterior node locations for all nodes within a clade}
\usage{
plot_clade(
  geo,
  tips,
  burnin = 0.1,
  model = "PALEOMAP",
  modern_coords = TRUE,
  map_age = NULL,
  bbox = NULL,
  show_points = FALSE,
  pt_col = "#555555",
  pt_size = 0.4,
  pt_alpha = 0.15,
  show_tips = TRUE,
  show_median = TRUE,
  med_size = 3,
  label_nodes = FALSE,
  maps = NULL
)
}
\arguments{
\item{geo}{A geo_ancstates object.}

\item{tips}{Character vector. Tip names defining the clade.}

\item{burnin}{Numeric 0-1. Burn-in fraction (default 0.1).}

\item{model}{Character. rgplates model (default "PALEOMAP").}

\item{modern_coords}{Logical. If TRUE (default), reconstruct coordinates to
present day to avoid land/sea mismatch.}

\item{map_age}{Numeric or NULL. Used when modern_coords = FALSE.
NULL = clade root age.}

\item{bbox}{Numeric c(lon_min, lon_max, lat_min, lat_max).
NULL (default) = full world map.}

\item{show_points}{Logical. Plot full MCMC sample as points (default FALSE).}

\item{pt_col}{Character. Point colour (default "#555555").}

\item{pt_size}{Numeric. Point size (default 0.4).}

\item{pt_alpha}{Numeric 0-1. Point transparency (default 0.15).}

\item{show_median}{Logical. Plot posterior median per node (default TRUE).}

\item{med_size}{Numeric. Median symbol size (default 3).}

\item{label_nodes}{Logical. Add node name labels (default FALSE).}

\item{maps}{A geo_paleomaps object. Avoids GWS calls.}
}
\value{
A ggplot2 object.
}
\description{
Plot posterior node locations for all nodes within a clade
}
\examples{
geo <- read_geo_ancstates("VR.AncStates.txt")
plot_clade(geo, tips = c("A_ligtu_ligtu_PCM32", "A_ligtu_simsii_OTN173"))
plot_clade(geo, tips = c("A_ligtu_ligtu_PCM32", "A_ligtu_simsii_OTN173"),
                  modern_coords = FALSE, maps = maps)
}
