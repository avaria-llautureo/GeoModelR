% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualise.R
\name{plot_timeinterval}
\alias{plot_timeinterval}
\title{Plot posterior node locations for all nodes within a time window}
\usage{
plot_timeinterval(
  geo,
  age_min = 0,
  age_max = Inf,
  burnin = 0.1,
  model = "PALEOMAP",
  modern_coords = TRUE,
  map_age = NULL,
  show_tips = TRUE,
  bbox = NULL,
  show_points = FALSE,
  pt_col = "#555555",
  pt_size = 0.4,
  pt_alpha = 0.15,
  show_median = TRUE,
  med_size = 3,
  maps = NULL
)
}
\arguments{
\item{geo}{A geo_ancstates object.}

\item{age_min}{Numeric. Lower bound of the time window in Ma (default 0).}

\item{age_max}{Numeric. Upper bound in Ma (default Inf = all nodes).}

\item{burnin}{Numeric 0-1. Burn-in fraction (default 0.1).}

\item{model}{Character. rgplates model (default "PALEOMAP").}

\item{modern_coords}{Logical. If TRUE (default), reconstruct coordinates to
present day to avoid land/sea mismatch across ages.}

\item{map_age}{Numeric or NULL. Used when modern_coords = FALSE.
Palaeomap age in Ma; NULL = midpoint of the window.}

\item{show_tips}{Logical. Include tip nodes in the plot (default TRUE).
Set FALSE to show only internal nodes.}

\item{bbox}{Numeric c(lon_min, lon_max, lat_min, lat_max).
NULL (default) = full world map.}

\item{show_points}{Logical. Plot full MCMC sample as points (default FALSE).}

\item{pt_col}{Character. Point colour (default "#555555").}

\item{pt_size}{Numeric. Point size (default 0.4).}

\item{pt_alpha}{Numeric 0-1. Point transparency (default 0.15).}

\item{show_median}{Logical. Plot posterior median per node (default TRUE).}

\item{med_size}{Numeric. Median symbol size (default 3).}

\item{maps}{A geo_paleomaps object. Avoids GWS calls.}
}
\value{
A ggplot2 object.
}
\description{
Plot posterior node locations for all nodes within a time window
}
\examples{
geo <- read_geo_ancstates("VR.AncStates.txt")
plot_timeinterval(geo, age_min = 0, age_max = 8)
plot_timeinterval(geo, age_min = 0, age_max = 8, modern_coords = FALSE, maps = maps)
}
